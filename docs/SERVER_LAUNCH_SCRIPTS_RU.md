# Скрипты Запуска Dedicated Server - Документация

## Обзор

Созданы комплексные скрипты для запуска и тестирования выделенного сервера Echoes MMO на Windows.

## Файлы

### 1. LaunchDedicatedServer.bat (Основной скрипт)

**Описание:** Традиционный Windows batch-скрипт для запуска сервера.

**Особенности:**
- Переменные окружения для путей (UNREAL_EDITOR, PROJECT_FILE, MAP_NAME)
- Поддержка обоих режимов: DedicatedSystem и RegionalCluster
- Полная проверка ошибок через ERRORLEVEL
- Примеры для нескольких инстансов в комментариях
- Флаг -log для вывода в консоль

**Настройка:**
```batch
REM Путь к UnrealEditor.exe
set "UNREAL_EDITOR=C:\Program Files\Epic Games\UE_5.3\Engine\Binaries\Win64\UnrealEditor.exe"

REM Путь к .uproject файлу
set "PROJECT_FILE=%~dp0Client\Echoes\Echoes.uproject"

REM Имя карты (без .umap)
set "MAP_NAME=GameLevel"

REM Тип сервера
set "SERVER_TYPE=DedicatedSystem"

REM Порт
set "SERVER_PORT=7777"

REM ID системы (GUID)
set "SYSTEM_ID=00000000-0000-0000-0000-000000000001"
```

**Использование:**
```batch
cd tools
LaunchDedicatedServer.bat
```

### 2. LaunchDedicatedServer.ps1 (PowerShell версия)

**Описание:** Современный PowerShell скрипт с параметрами командной строки.

**Особенности:**
- Параметры командной строки (не нужно редактировать файл)
- Цветной вывод в консоль
- Встроенная справка (-Help)
- Улучшенная обработка ошибок

**Использование:**
```powershell
# Справка
.\tools\LaunchDedicatedServer.ps1 -Help

# Запуск с параметрами по умолчанию
.\tools\LaunchDedicatedServer.ps1

# Кастомная конфигурация
.\tools\LaunchDedicatedServer.ps1 `
    -ServerType DedicatedSystem `
    -Port 7777 `
    -SystemId "00000000-0000-0000-0000-000000000001"

# Региональный кластер
.\tools\LaunchDedicatedServer.ps1 `
    -ServerType RegionalCluster `
    -RegionId "00000000-0000-0000-0000-000000000010"
```

### 3. QuickLaunch.bat (Быстрое меню)

**Описание:** Интерактивное меню для быстрого запуска типичных сценариев.

**Опции меню:**
1. Одиночный сервер системы (порт 7777)
2. Два сервера систем (порты 7777, 7778) - в отдельных окнах
3. Региональный кластер (порт 7777)
4. Произвольная конфигурация (визард)
5. Выход

**Использование:**
```batch
tools\QuickLaunch.bat
```

### 4. README.md (Документация)

Полная документация по использованию всех скриптов.

## Параметры Командной Строки

Скрипты передают следующие параметры серверу:

| Параметр | Описание | Обязательный | Пример |
|----------|----------|--------------|--------|
| `-ServerType=` | Тип сервера | Нет (по умолчанию: DedicatedSystem) | `-ServerType=RegionalCluster` |
| `-SystemId=` | GUID солнечной системы | Для DedicatedSystem | `-SystemId=00000000-0000-0000-0000-000000000001` |
| `-RegionId=` | GUID региона | Для RegionalCluster | `-RegionId=00000000-0000-0000-0000-000000000010` |
| `-Port=` | Порт игрового сервера | Нет (по умолчанию: 7777) | `-Port=7778` |
| `-server` | Запуск как dedicated server | Да | (только флаг) |
| `-log` | Показать консоль с логами | Рекомендуется | (только флаг) |

## Парсинг Параметров

Параметры парсятся в классе `UEchoesServerManagementSubsystem` в методе `Initialize()`:

```cpp
// Тип сервера (DedicatedSystem или RegionalCluster)
FParse::Value(FCommandLine::Get(), TEXT("ServerType="), CommandLineServerType);

// ID солнечной системы (для DedicatedSystem)
FParse::Value(FCommandLine::Get(), TEXT("SystemId="), SystemIdStr);

// ID региона (для RegionalCluster)
FParse::Value(FCommandLine::Get(), TEXT("RegionId="), RegionIdStr);

// Порт
FParse::Value(FCommandLine::Get(), TEXT("Port="), Port);
```

## Режимы Работы

### DedicatedSystem (по умолчанию)

Сервер управляет одной солнечной системой.

**Требуется:**
- `-SystemId=` (GUID системы)

**Пример:**
```batch
set SERVER_TYPE=DedicatedSystem
set SYSTEM_ID=00000000-0000-0000-0000-000000000001
LaunchDedicatedServer.bat
```

**Логирование:**
```
[LogEchoesServer] Server Type: DedicatedSystem
[LogEchoesServer] Solar System ID: 00000000-0000-0000-0000-000000000001
```

### RegionalCluster

Сервер управляет несколькими системами в регионе.

**Требуется:**
- `-RegionId=` (GUID региона)

**Пример:**
```batch
set SERVER_TYPE=RegionalCluster
set REGION_ID=00000000-0000-0000-0000-000000000010
LaunchDedicatedServer.bat
```

**Логирование:**
```
[LogEchoesServer] Server Type: RegionalCluster
[LogEchoesServer] Region ID: 00000000-0000-0000-0000-000000000010
```

## Проверка Ошибок

Все скрипты проверяют `ERRORLEVEL` после завершения сервера:

```batch
if errorlevel 1 (
    echo [ERROR] Server crashed or exited with error code: %ERRORLEVEL%
    echo Common issues:
    echo   - Missing or corrupt game files
    echo   - Port already in use
    echo   - Invalid SystemId or RegionId
    echo   - Backend connection failure
    pause
    exit /b %ERRORLEVEL%
)
```

**Возможные ошибки:**
- **Код 0:** Нормальное завершение
- **Код ≠ 0:** Произошла ошибка
  - Порт уже используется
  - Неверный формат GUID
  - Backend недоступен
  - Отсутствуют файлы игры

## Запуск Нескольких Инстансов

### Сценарий 1: Два сервера систем (DedicatedSystem)

**Терминал 1:**
```batch
set SERVER_PORT=7777
set SYSTEM_ID=00000000-0000-0000-0000-000000000001
LaunchDedicatedServer.bat
```

**Терминал 2:**
```batch
set SERVER_PORT=7778
set SYSTEM_ID=00000000-0000-0000-0000-000000000002
LaunchDedicatedServer.bat
```

Или с PowerShell:
```powershell
# Терминал 1
.\tools\LaunchDedicatedServer.ps1 -Port 7777 -SystemId "00000000-0000-0000-0000-000000000001"

# Терминал 2
.\tools\LaunchDedicatedServer.ps1 -Port 7778 -SystemId "00000000-0000-0000-0000-000000000002"
```

### Сценарий 2: Quick Launch (Автоматически)

```batch
tools\QuickLaunch.bat
(выбрать опцию 2)
```

Это откроет два отдельных окна с серверами на портах 7777 и 7778.

## Ожидаемый Вывод

При успешном запуске:

```
========================================================================
 Echoes MMO - Dedicated Server Launcher
========================================================================

[OK] Unreal Editor found
[OK] Project file found

[INFO] Launching DedicatedSystem server
[INFO] System ID: 00000000-0000-0000-0000-000000000001
[INFO] Port: 7777
[INFO] Map: GameLevel

========================================================================
 Starting Dedicated Server...
========================================================================

Command: "C:\...\UnrealEditor.exe" "C:\...\Echoes.uproject" GameLevel -server -log -Port=7777 -ServerType=DedicatedSystem -SystemId=00000000-0000-0000-0000-000000000001

Press Ctrl+C to stop the server

[LogEchoesServer] EchoesServerManagementSubsystem initialized
[LogEchoesServer] Using ServerType from command line: DedicatedSystem
[LogEchoesServer] Using SolarSystemId from command line: 00000000-0000-0000-0000-000000000001
[LogEchoesServer] ╔══════════════════════════════════════════════════════════╗
[LogEchoesServer] ║    SERVER CONFIGURATION                                  ║
[LogEchoesServer] ╚══════════════════════════════════════════════════════════╝
[LogEchoesServer] Server Type: DedicatedSystem
[LogEchoesServer] Instance ID: <generated-guid>
[LogEchoesServer] Game Port: 7777
[LogEchoesServer] Solar System ID: 00000000-0000-0000-0000-000000000001

[LogTemp] ┌─────────────────────────────────────────────────────────────┐
[LogTemp] │ MATERIALIZING SYSTEM: <SystemName>                          │
[LogTemp] ├─────────────────────────────────────────────────────────────┤
[LogTemp] │ System ID: 00000000-0000-0000-0000-000000000001            │
[LogTemp] │ Star Class: G2V                                             │
[LogTemp] │ Security Status: 1.00                                       │
[LogTemp] ├─────────────────────────────────────────────────────────────┤
[LogTemp] │ OBJECT STATISTICS                                           │
[LogTemp] ├─────────────────────────────────────────────────────────────┤
[LogTemp] │ Celestial Bodies:                                           │
[LogTemp] │   ⭐ Stars:            1                                     │
[LogTemp] │   🌍 Planets:         10                                     │
[LogTemp] │   🌙 Moons:           24                                     │
...

[SERVER] Objects: 1 Star (Class G2V) | 10 Planets | 24 Moons | 5 Stations | 4 Stargates
[SERVER] Status: Generation Successful
```

## Устранение Неполадок

### Ошибка: "Unreal Editor not found"

**Решение:** Обновите путь к UnrealEditor.exe:
- В batch: измените переменную `UNREAL_EDITOR`
- В PowerShell: используйте параметр `-UnrealEditorPath`

Типичные расположения:
- `C:\Program Files\Epic Games\UE_5.3\Engine\Binaries\Win64\UnrealEditor.exe`
- `C:\Program Files\Epic Games\UE_5.4\Engine\Binaries\Win64\UnrealEditor.exe`

### Ошибка: "Project file not found"

**Решение:** Убедитесь, что запускаете скрипт из корня репозитория или директории tools.

### Сервер сразу падает

**Возможные причины:**
- Порт уже используется (попробуйте другой порт)
- Неверный формат SystemId или RegionId (должен быть GUID)
- Backend сервер не запущен или недоступен
- Отсутствуют ресурсы игры

Проверьте вывод консоли (с флагом `-log`) на наличие конкретных сообщений об ошибках из `LogEchoesServer`.

### Ошибка PowerShell execution policy

Если получаете "script cannot be loaded because running scripts is disabled", выполните:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## Интеграция с Backend

Dedicated server автоматически:
1. Регистрируется на backend API при старте
2. Отправляет heartbeat каждые 30 секунд
3. Получает конфигурацию мира (системы, планеты и т.д.)
4. Отменяет регистрацию при корректном завершении

Убедитесь, что backend API запущен и доступен перед запуском dedicated server.

## Дополнительные Примеры

### Запуск с другой картой

```batch
set MAP_NAME=EntryMap
LaunchDedicatedServer.bat
```

Или PowerShell:
```powershell
.\tools\LaunchDedicatedServer.ps1 -MapName EntryMap
```

### Headless режим (без консоли)

PowerShell:
```powershell
.\tools\LaunchDedicatedServer.ps1 -NoLog
```

### Полная кастомизация

PowerShell:
```powershell
.\tools\LaunchDedicatedServer.ps1 `
    -ServerType DedicatedSystem `
    -Port 8888 `
    -SystemId "AAAAAAAA-BBBB-CCCC-DDDD-EEEEEEEEEEEE" `
    -MapName GameLevel `
    -UnrealEditorPath "D:\UnrealEngine\UE_5.4\Engine\Binaries\Win64\UnrealEditor.exe"
```

## Заключение

Скрипты запуска dedicated server предоставляют:

✅ **Гибкость:**
- Batch и PowerShell версии
- Интерактивное меню для быстрого тестирования
- Поддержка нескольких инстансов

✅ **Надежность:**
- Полная проверка ошибок
- Валидация путей и конфигурации
- Понятные сообщения об ошибках

✅ **Документация:**
- Подробная документация
- Примеры для всех сценариев
- Руководство по устранению неполадок

Все скрипты готовы к использованию и тестированию!
